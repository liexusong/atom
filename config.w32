// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("atom", "for atom support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("atom", "enable atom support", "no");

if (PHP_ATOM != "no") {
	EXTENSION("atom", "atom.c");
}

